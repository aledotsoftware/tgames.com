{
  "name": "tgames",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run update-i18n && nuxt build",
    "dev": "npm run update-i18n && nuxt dev",
    "generate": "npm run update-i18n && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "update-i18n": "node scripts/fetch-languages.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "ioredis": "^5.9.3",
    "mysql2": "^3.17.4",
    "nuxt": "^3.10.0",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^10.2.3"
  }
}
